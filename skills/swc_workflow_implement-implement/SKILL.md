---
name: swc_workflow_implement-implement
description: Implement stage of the implementation workflow — work against the approved spec, document decisions in context.md, inner loop until spec passes. Second stage of the implementation workflow. Use when invoked by swc_workflow_implement or via /swc-workflow-implement-implement.
allowed-tools: Read, Write, Edit, Glob, Bash
---

# Implement — Implement Stage

**This is a placeholder.** Announce stage entry, confirm the work item, and exit.

> "Implement stage — placeholder. Work item: [N]: [name]. Workload folder: [folder].
> This stage will: work against the approved spec, document decisions in context.md as work happens, flag scope observations, and loop (implement → run spec → fix) until the spec passes or the agent is genuinely blocked.
> Placeholder complete — returning to orchestrator."

Return control to the orchestrator.
